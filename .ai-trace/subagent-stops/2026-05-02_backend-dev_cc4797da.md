# backend-dev

- ts: 2026-05-02T20:52:22Z
- agent: backend-dev
- session: cc4797da

## Payload

```json
{"session_id":"cc4797da-fa4c-49d6-b19c-a832635e1fb2","transcript_path":"C:\\Users\\iyasi\\.claude\\projects\\D--repos-BinanceBot\\cc4797da-fa4c-49d6-b19c-a832635e1fb2.jsonl","cwd":"D:\\repos\\BinanceBot","permission_mode":"auto","agent_id":"a9dc89f6c4b25dc75","agent_type":"backend-dev","hook_event_name":"SubagentStop","stop_hook_active":false,"agent_transcript_path":"C:\\Users\\iyasi\\.claude\\projects\\D--repos-BinanceBot\\cc4797da-fa4c-49d6-b19c-a832635e1fb2\\subagents\\agent-a9dc89f6c4b25dc75.jsonl","last_assistant_message":"## Rapor\n\n### Bug Tespit Lokasyonu\n`src/Application/Portfolio/Queries/GetPortfolioSummary/GetPortfolioSummaryQuery.cs:69-98` (orijinal sürüm)\n\n### Kök Neden\nHandler `VirtualBalance.CurrentBalance` snapshot'ını **tek otorite kaynak** olarak okuyordu, sonra `trueEquity = CurrentBalance + openCostBasis + unrealized` hesaplıyordu. Bu formül matematiksel olarak doğru — **ama yalnızca CurrentBalance gerçekten \"settled cash with open notional deducted\" ise**.\n\nPratikte snapshot drift edebiliyor:\n- Loop 18 öncesi PaperFillSimulator farklı semantikle (clamp-at-zero) çalışıyordu\n- Loop 32 ADR-0020 cash-symmetric simulator'a geçiş yapıldı\n- Reset ile fill arasındaki race window'ları\n- DB row mevcut runtime'dan farklı bir önceki bot binary tarafından yazılmış olabilir\n\nSenin senaryonda CurrentBalance $355.14 (beklenen $197.43, fark +$157.71 phantom) — UI bu phantom'u `trueEquity − start` üzerinden \"Toplam Net K/Z +$155\" olarak yansıttı.\n\n**PaperFillSimulator (`src/Infrastructure/Trading/Paper/PaperFillSimulator.cs:172-189`) ve `VirtualBalance.ApplyFill` doğru yazılmış** — testlerle locked (`PaperFillSimulator_BuyThenSellSameSize_CashDelta_EqualsMinusTwoQuoteFees`). Bug **persisted snapshot'ın drift'inden** kaynaklanıyor, runtime fill mantığından değil.\n\n### Yapılan Değişiklik\n`src/Application/Portfolio/Queries/GetPortfolioSummary/GetPortfolioSummaryQuery.cs` — Loop 84 cash-bug fix:\n\n```\nledgerCash = StartingBalance\n           + Σ closed.RealizedPnl       (entry+exit komisyon zaten net)\n           − Σ open.AverageEntryPrice × Quantity   (open BUY notional)\n           − Σ open.EntryCommission                 (open BUY fee)\n\ntrueEquity = ledgerCash + Σ open.cost-basis + Σ open.UnrealizedPnl\nNetPnl     = trueEquity − StartingBalance\nNetAfterFees = realizedAllTime + unrealizedTotal − openCommission\n```\n\n`VirtualBalance.CurrentBalance` artık UI'in headline değeri için **okunmuyor**. Snapshot drift mevcut bozuk row'da self-heal sağlanır. `GetBalances` query (admin-only) snapshot'ı göstermeye devam ediyor — operatör drift'i görebilir.\n\n### Test Sonucu\n- `tests/Tests/Application/Portfolio/GetPortfolioSummaryQueryTests.cs`:\n  - **YENİ**: `LedgerCash_DerivesFromPositions_NotFromVirtualBalanceSnapshot` — kullanıcı senaryosunu birebir simüle ediyor (start $500, realized −$0.61, 3 open notional $300.53, open komisyon $0.23, snapshot bilerek $355.14'e drift edilmiş). Beklenen ledgerCash=$198.63, trueEquity=$499.16, NetPnl=−$0.84.\n  - **GÜNCEL**: `OpenPositionWithUnrealizedPnl_TrueEquityExceedsCash`, `ClosedPositions_ProduceWinLossWinRate` — yeni ledger semantiğine göre beklenen değerler düzeltildi (cash = $80, $103.5).\n  - 3 invariant testi (`NetPnl_Invariant_*`) zaten cash-grounded netPnl identity'sini koruyordu — değişiklik gerektirmedi.\n- **Toplam: 321/321 test geçti, 0 hata.**\n\n### PM Deploy Sonrası DB Reset\nYeni handler artık snapshot'a bağımlı olmadığından **manuel DB UPDATE şart değil** — UI yeni iterasyonun ilk request'inde doğru değeri gösterecek. Yine de uzun vadeli temizlik için PM dilerse:\n\n```sql\n-- Mevcut Paper iterasyonun ledger-doğru CurrentBalance/Equity'sini snapshot'a yaz\nWITH closedRealized AS (\n    SELECT ISNULL(SUM(RealizedPnl), 0) AS Realized\n    FROM Positions WHERE Mode = 1 AND Status = 2\n),\nopenLedger AS (\n    SELECT ISNULL(SUM(AverageEntryPrice * Quantity), 0) AS Notional,\n           ISNULL(SUM(EntryCommission), 0) AS OpenCommission,\n           ISNULL(SUM(UnrealizedPnl), 0) AS Unrealized\n    FROM Positions WHERE Mode = 1 AND Status = 1\n)\nUPDATE VirtualBalances\nSET CurrentBalance = StartingBalance + (SELECT Realized FROM closedRealized)\n                                     - (SELECT Notional FROM openLedger)\n                                     - (SELECT OpenCommission FROM openLedger),\n    Equity = StartingBalance + (SELECT Realized FROM closedRealized)\n                             + (SELECT Unrealized FROM openLedger)\n                             - (SELECT OpenCommission FROM openLedger),\n    UpdatedAt = SYSDATETIMEOFFSET()\nWHERE Mode = 1;\n```\n\nSenin senaryonda bu update sonucu CurrentBalance ≈ $198.63 (snapshot ile UI artık tutarlı), Equity ≈ $499.16 olur.\n\n### Build Durumu\n- Application/Domain projesi: **build başarılı** (0 uyarı, 0 hata)\n- Tests projesi: **build + run başarılı** (321/321)\n- Api projesi PID 15732 tarafından kilitli — full sln build PM'in görevi (bot durdurulduktan sonra)\n\n### Değiştirilen Dosyalar (mutlak yol)\n- `D:\\repos\\BinanceBot\\src\\Application\\Portfolio\\Queries\\GetPortfolioSummary\\GetPortfolioSummaryQuery.cs`\n- `D:\\repos\\BinanceBot\\tests\\Tests\\Application\\Portfolio\\GetPortfolioSummaryQueryTests.cs`\n\nPush yapmadım — PM commit edecek."}
```
